<div class="valoritization" id="root">
  <%= javascript_tag do %>
    var retrieved_tasks = <%=@tasks.to_json.html_safe %>
    var matrix_infos = <%=@matrix.attributes.merge(read_only: current_user&.id != @matrix.user_id).to_json.html_safe %>
  <% end %>
</div>
<script src="/bundle.js"></script>

<%= link_to 'Edit', edit_matrix_path(@matrix), class: 'btn btn-default' %>
<%= link_to 'Back', matrices_path, class: 'btn btn-default' %>
